#%PAM-1.0
# Mock PAM configuration for testing - password authentication
# Used by pam_wrapper to simulate password-based auth
#
# To simulate success: Set environment variable TEST_PASSWORD=correct
# To simulate failure: Set environment variable TEST_PASSWORD=wrong

# For now, just permit (we'll enhance this with pam_echo later)
auth     sufficient   pam_permit.so
account  sufficient   pam_permit.so
password sufficient   pam_permit.so
session  sufficient   pam_permit.so
